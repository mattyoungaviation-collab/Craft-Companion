import { useState } from 'react'; import { useNavigate } from 'react-router-dom'; import { login } from '../services/api';
export default function SignIn(){const nav=useNavigate();const [username,setU]=useState('');const [password,setP]=useState('');const [e,setE]=useState('');
return <form onSubmit={async(ev)=>{ev.preventDefault();try{await login({username,password});nav('/home');}catch(err:any){setE(err.message)}}} className='max-w-md mx-auto mt-12 space-y-3'><input className='w-full p-2 rounded bg-slate-900 border border-slate-700' placeholder='Username' value={username} onChange={e=>setU(e.target.value)}/><input type='password' className='w-full p-2 rounded bg-slate-900 border border-slate-700' placeholder='Password' value={password} onChange={e=>setP(e.target.value)}/>{e&&<p className='text-red-400'>{e}</p>}<button className='w-full bg-blue-600 rounded p-2'>Sign In</button></form>}
